spring:
  application:
    name: vendor-service

  profiles:
    active: prod

server:
  port: 9101

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:19090/eureka/